# Search — Legacy UI

This file previously documented the search implementation in the legacy AWX AngularJS/PatternFly UI.

**The active Forail frontend is in a separate repository:** [forail-frontend](https://github.com/forail-platform/forail-frontend)

The current frontend uses TanStack Query with server-side filtering. See `forail-frontend/src/api/hooks/` for the implementation.
